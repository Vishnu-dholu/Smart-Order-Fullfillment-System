DATABASE_URL="postgresql://<user>:<password>@<host>/<db>?sslmode=require"
PORT=8086

# Email Configuration (use an app password if needed)
SMTP_EMAIL="you@example.com"
SMTP_PASSWORD="your_app_password"

