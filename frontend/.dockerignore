# Ignore local env files — build-time env vars are passed as --build-arg by CI
# This prevents a developer's local .env from overriding CI-injected values
.env
.env.local
.env.*.local

# Development artifacts
node_modules
dist
npm-debug.log*

# Editor / OS files
.DS_Store
*.swp
